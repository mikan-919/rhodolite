## 1. Struct 宣言索引

- [ ] 1.1 `typecheck` モジュールの公開境界と、重複・非重複の宣言フィールドを固定する単体テストを追加する
- [ ] 1.2 正準 struct 名から宣言フィールド集合を作り、重複宣言フィールドを決定的な順序で報告する

## 2. Struct リテラル検査

- [ ] 2.1 宣言済み・未知・非 struct・順序違い・不足・余分・重複フィールドの各シナリオを単体テストにする
- [ ] 2.2 全ての実行可能本体を走査し、struct リテラルの型名とフィールド集合を宣言索引へ照合する

## 3. Bare struct と字句スコープ

- [ ] 3.1 フィールド0個・フィールドあり・引数・`let`・`for`・`self` によるシャドーイングを単体テストにする
- [ ] 3.2 既存の束縛規則に従ってローカル集合を枝ごとに管理し、未シャドーの bare struct 名を検査する

## 4. CLI 統合

- [ ] 4.1 モジュール解決後かつ要求解析前に `typecheck::check` を実行し、診断があれば評価せず失敗終了する
- [ ] 4.2 未到達関数内の不正な生成、モジュールを跨ぐ正準 struct 名、有効な既存プログラムを CLI 結合テストで検証する

## 5. 文書化と検証

- [ ] 5.1 `src/eval.rs` の古い ponytail と `docs/overview.md` を、struct shape 検査の保証範囲と残る型検査が分かる記述へ更新する
- [ ] 5.2 `cargo fmt --check`、`cargo clippy -- -D warnings`、全テスト、正典プログラム、OpenSpec validate を実行する
