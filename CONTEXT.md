# Rhodolite 用語集

このファイルは**用語集**であり、仕様書でも設計メモでもない。実装の詳細は書かない。
設計判断は `docs/adr/` に、構想スナップショットは Obsidian vault (`2_Products/rhodolite/`) に置く。

## エフェクト (effect)

**ambient に置かれた trait**。それ以上のものではない。
`effect` という宣言構文は存在しない — 宣言するのはただの trait であり、
その trait を ambient に置いた瞬間、それはエフェクトとして振る舞う。

したがって「エフェクトを宣言する」という言い方はしない。正しくは
「trait を宣言する」「trait を ambient に置く」。

trait を ambient に置くと、通常の trait と2点だけ違う振る舞いになる:

- **推移的** … 呼び出しチェーンの何階層下からでも届く。間の関数は書き換え不要
- **要求が推論される** … どの trait を要求するかを関数に書かない。コンパイラが求める

「純粋性の追跡」ではない。追跡するのは**ambient に置かれた trait だけ**であり、
すべての IO を追うものではない。売り物は純粋性ではなく**差し替え可能性**。

系譜としては Scala の `given`/`using` に最も近く、差分は
**経由するすべての関数に `using` を書かなくてよい**(全推論)の一点。

## ハンドラ (handler)

ambient に置かれた trait 実装。**ただの `impl`** であり、専用の宣言構文はない。
「ハンドラを書く」= 「trait を impl する」。

rhodolite のハンドラは **tail-resumptive 限定** — 中断した処理を1回だけ再開してすぐ返る。
継続を保存しない/複数回再開しない。したがってハンドラで
async/await・ジェネレータ・バックトラッキングは書けない。これらは非目標。

## エフェクト変数 / エフェクト多相

高階関数のエフェクトが「渡された関数次第」で決まる性質。
`map` に Db を起こす関数を渡せば、その `map` 呼び出しは Db を起こす。

型に正直に書くと `fn map(xs: [A], f: (A -> B / e)) -> [B] / e` の `e` が現れる。
**この `e` をユーザーの語彙に出さないことが rhodolite の主要な設計目標のひとつ。**

## 分割コンパイル

**この語は使わない。**曖昧で、指しうる3つの意味の代償が全く違うため。
必ず次のいずれかに分解して呼ぶ:

| 呼び方 | 意味 | rhodolite での扱い |
|---|---|---|
| モジュール分割 | ソースを複数ファイル/名前空間に分けて書ける | **やる** |
| 増分ビルド | 変更箇所だけ再コンパイルして速い | **やる** |
| モジュール単独型検査 | 他モジュールの本体を見ずに型・エフェクトが確定する | **やらない**(whole-program 前提) |

## Head(環境構築子)

`:` の左に来るもの。**環境を構築して、続く1文をその環境で実行する**もの。
`if` / `if let ... = ...` / `for x in xs` / アロケータ値 / ハンドラの束 がすべてこれ。

Head が宣言するのは [導入する束縛, 実行回数, ambient の変更, 環境の寿命] の4つ。

## 第二級ブロック

`{ ... }` そのもの。**いま・ここで**環境が構築されるので捕捉が逃げない。
値ではないため変数に入らず、関数に渡らず、返り値にもならない。
対して `fn(...) { ... }` だけが環境構築を遅延する = 捕捉する = 第一級の値。

## 3つの位置

ambient に関わる記述は、ソース上の3箇所にしか現れない。

| 位置 | 書くもの | 例 |
|---|---|---|
| **宣言** | trait | `trait Database { fn save(u: User -> unit) }` |
| **提供** | Head + ブロック | `Database(db): { handle() }` |
| **使用** | trait 名で修飾した呼び出し | `Database::save(u)` |

**この3箇所以外には1文字も現れない。**経由するだけの関数は無記述。
関数がどの ambient を要求するかは推論され、**書かない**(IDE がゴーストで見せる)。

## スロット (slot) `@Name`

**役割の名前**。「読み取り用のDB」「監査ログの出力先」のような役を指す名前であって、
実装ではない。何をそこに挿すかは提供側が決める。

同じ trait の実装を同時に2つ ambient に置きたいときに使う:

```rhodolite
@Replica.find(id)     // 読みはレプリカ役から
@Primary.save(u)      // 書きはプライマリ役へ
```

- ある trait の ambient が**1つだけ**なら、スロット名は要らない(`Database.find(id)`)
- **2つ以上**開いていると曖昧エラーになり、スロット名での名指しを要求される

規則は「**曖昧なら名指せ**」の1本だけ。単一の場合の記述量はゼロ。

Spring の `@Qualifier("replica")` に相当するが、文字列ではなく言語の一級の宣言物。

## ambient

環境の束縛のうち、**関数呼び出しで切れないもの**。

環境に置ける束縛は2種類しかない:

| | 例 | 呼び出し先に届くか |
|---|---|---|
| 通常の束縛 | `let db = ...` | 届かない |
| **ambient 束縛** | `pg@Primary: { ... }` | **届く** |

この「切れない」性質が**推移性**であり、rhodolite の一点突破のすべて。
DI コンテナが存在する理由(依存を通すために経由する全関数を書き換える作業)は、
ambient 束縛があれば発生しない。

引数と違って推移的、グローバル変数と違って**スコープが静的で型に出る**
(与え忘れはコンパイルエラー)。

ハンドラも(将来の)アロケータも ambient として渡る。

## 語らない語

- **`'a` / ライフタイム注釈** … ユーザー語彙に存在させない。これが元々の出発点
- **DIコンテナ / モック** … 言語がエフェクトを見られないことの回避策。rhodolite には対応物がない
